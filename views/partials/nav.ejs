  <% if (!user) { %>
    <!-- <link rel="stylesheet" href="/stylesheets/siwg.css"> -->
  <% } %>
</head>
<body>

<header>
  <p class="logo"><a href="/brewguides">Brew Guides</a></p>
  <nav>
    <ul class="nav_links">
      <% if (user?.profile.avatar) { %>
        <li>
          <img height="25px" src="<%= user.profile.avatar %>" alt="<%= user.profile.name %> avatar">
        </li>
      <% } %>
      <li>
        <% if (user) { %>
          <a href="/auth/logout">Logout</a>
        <% } else { %>
          <a href="/auth/google">Login</a>
        <% } %>
      </li>
      
        
        
    </ul>
  </nav>
</header>